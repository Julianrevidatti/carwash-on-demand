<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:material="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TITULO -->
    <TextView
        android:text="Mi agenda"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="4dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:text="Maneja tus reservas facilmente"
        android:textSize="14sp"
        android:textColor="#888"
        android:layout_marginBottom="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- SEGMENTED TABS -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        material:tabIndicatorHeight="0dp"
        material:tabBackground="@drawable/tab_selector"
        material:tabTextColor="#888"
        material:tabSelectedTextColor="@android:color/white">

        <com.google.android.material.tabs.TabItem
            android:text="Programado"/>

        <com.google.android.material.tabs.TabItem
            android:text="Finalizado"/>

        <com.google.android.material.tabs.TabItem
            android:text="Cancelado"/>
    </com.google.android.material.tabs.TabLayout>

    <!-- LISTA -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/bookingsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="16dp"/>
</LinearLayout>